apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test-allow-es-fb
  namespace: default
  labels:
    cluster: test
    filebeat.k8s.webcenter.fr: "true"
  annotations:
    filebeat.k8s.webcenter.fr: "true"
spec:
  egress:
    - to:
        - podSelector:
            matchLabels:
              cluster: test
              elasticsearch.k8s.webcenter.fr: true
          namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: elastic
      ports:
        - port: 9200
          protocol: TCP
  policyTypes:
    - Egress
  podSelector:
    matchLabels:
      cluster: test
      filebeat.k8s.webcenter.fr: true