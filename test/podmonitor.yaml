apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: logmanagement-dev-es
  labels:
    socle: logmanagement-dev
    cluster: logmanagement-dev
spec:
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-logmanagement-dev-exporter
  podMetricsEndpoints:
  - port: exporter